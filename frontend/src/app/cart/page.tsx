import { CartScreen } from "@/features/cart/cart-screen";

export const metadata = {
  title: "My Cart | Urja Basket",
  description: "Review items in your cart and proceed to checkout.",
};

export default function CartPage() {
  return <CartScreen />;
}
